{
    "builds": [{
        "src": "bookmyseat/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "50mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "bookmyseat/wsgi.py"
        }
    ]
}
